# WebBank
An online banking demonstration.

The business requirements are kept as a VitePress wiki in `doc/` (`npm install`
then `npm run dev`). It is documentation only and is not part of the Maven
build.
